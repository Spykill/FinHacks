import numpy
import sys
import math
'''
create a list of 12 floats generated by normal RVs
and then normalize that list st. sum(lst) == 1
'''
def generate_list(stat_vec):
	'''
	stat_vec accept a vector of tuples of means and std deviation
	means is the expected propostion of purchases of a good,
	and std deviation is based on the probabiliy of that 
	category being a hobby.
	'''
	a = []
	for param in stat_vec:
		a.append(abs(numpy.random.normal(loc=param[0],scale=param[1],size=None)))
	return a
	
def normalize_list(lst):
	s = sum(lst)
	for i in range(len(lst)):
		lst[i] = lst[i]/s

a=generate_list([(0,1) for x in range(0,10)])
		

